# ePriceFlo - Variables de Entorno
# Copia este archivo a .env y configura tus valores

# ===== BASE DE DATOS =====
# Si no defines DATABASE_URL, se usa SQLite en data/prices.db (perfecto para local)
# En Railway/Producción, Railway inyecta automáticamente DATABASE_URL para PostgreSQL
# DATABASE_URL=postgresql://usuario:password@host:5432/database

# ===== SCRAPING =====
# Habilitar o deshabilitar el scraping automático
# En producción (Railway): déjalo en false y corre scraping local
# En local: ponlo en true si quieres hacer scraping
ENABLE_SCRAPING=false

# ===== API CONFIGURATION =====
# Puerto en el que corre la API (Railway usa PORT automáticamente)
PORT=8000

# ===== CORS =====
# Dominios permitidos para CORS (separados por coma)
# En producción, cambia esto a tu dominio real
# ALLOWED_ORIGINS=https://epriceflo.com,https://www.epriceflo.com

# ===== NOTAS =====
# - Para desarrollo local: solo necesitas ENABLE_SCRAPING=true
# - Para producción (Railway):
#   1. No necesitas configurar DATABASE_URL (Railway lo hace automáticamente)
#   2. ENABLE_SCRAPING debe ser false
#   3. Configura ALLOWED_ORIGINS con tu dominio real
